import numpy as np
import pandas as pd
import scipy.sparse as sp
import sklearn
from typing import List


class Classification(object):

    def __init__(self):
        self.x = 1
        data.groupby('model').apply(lambda group: sklearn.metrics.auc(group.FPR, group.TPR))

    def calculate(self, scores: pd.DataFrame):
        pass
    
    def plot(self):
        pass

    def flake(self):
        pass